name: Test no-platform extraction
on:
  push:
    branches:
      - testing-extraction-methods

jobs:
  test:
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/arm64
            runner: ubuntu-24.04-arm
            tag: arm64
          - platform: linux/386
            runner: ubuntu-24.04-arm
            tag: 386
          - platform: linux/ppc64le
            runner: ubuntu-24.04-arm
            tag: ppc64le
          - platform: linux/s390x
            runner: ubuntu-24.04-arm
            tag: s390x
          - platform: linux/amd64
            runner: ubuntu-latest
            tag: amd64
          - platform: linux/arm/v7
            runner: ubuntu-latest
            tag: armv7
          - platform: linux/arm64
            runner: ubuntu-latest
            tag: arm64-on-amd

    runs-on: ${{ matrix.runner }}
    name: "no-platform-extract ${{ matrix.tag }}"
    steps:
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
        id: buildx

      - name: Prepare
        run: mkdir -p scratch cache-output && date > scratch/buildstamp

      # Build WITH --platform (populate cache for that arch)
      - name: Populate cache (with --platform)
        run: |
          cat > scratch/Dancefile.inject << 'DEOF'
          FROM debian:bookworm-slim
          COPY buildstamp buildstamp
          RUN --mount=type=cache,target=/var/cache/apt,sharing=locked \
              --mount=type=cache,target=/var/lib/apt,sharing=locked \
              rm -f /etc/apt/apt.conf.d/docker-clean && \
              echo 'Binary::apt::APT::Keep-Downloaded-Packages "true";' > /etc/apt/apt.conf.d/keep-cache && \
              apt-get update && \
              apt-get install -y --no-install-recommends \
                curl wget git ca-certificates \
                build-essential gcc g++ make \
                python3 python3-dev && \
              echo "done"
          DEOF

          docker buildx build \
            --builder ${{ steps.buildx.outputs.name }} \
            --platform ${{ matrix.platform }} \
            -f scratch/Dancefile.inject \
            --progress=plain \
            scratch

      # Extract WITHOUT --platform (like the actual v3.3.1 action does)
      - name: "Extract: NO --platform (v3.3.1 style)"
        run: |
          cat > scratch/Dancefile.extract << 'DEOF'
          FROM ghcr.io/containerd/busybox:latest AS dance-extract
          COPY buildstamp buildstamp
          RUN --mount=type=cache,target=/var/cache/apt \
              mkdir -p /var/dance-cache/ \
              && cp -p -R /var/cache/apt/. /var/dance-cache/ || true
          FROM scratch
          COPY --from=dance-extract --chmod=u=rwX,go=rX /var/dance-cache /
          DEOF

          docker buildx build \
            --builder ${{ steps.buildx.outputs.name }} \
            -f scratch/Dancefile.extract \
            --tag dance:extract \
            --output type=local,dest=cache-output \
            --progress=plain \
            scratch

      - name: Verify
        run: |
          du -sh cache-output/ 2>/dev/null || echo "EMPTY"
          find cache-output/ -type f 2>/dev/null | wc -l
